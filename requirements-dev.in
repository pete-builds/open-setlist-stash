-r requirements.in
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
respx==0.23.1
ruff==0.16.4
mypy==2.3.1
# Type stubs for the `markdown` runtime dep. Without these mypy --strict fails
# on blog.py: the import is untyped (import-untyped) and markdown.markdown()
# then returns Any, which trips no-any-return. Stubs only, never in the image.
types-Markdown==3.10.2.20260712
