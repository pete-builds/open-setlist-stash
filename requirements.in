fastapi==0.139.2
# Direct pin on a transitive dep. fastapi only asks for starlette>=0.46.0, and
# the resolver was settling on 1.0.0, which carries two HIGH CVEs:
# CVE-2026-48818 (SSRF + NTLM credential theft via UNC paths, fixed 1.1.0) and
# CVE-2026-54283 (request.form() limits silently ignored, fixed 1.3.1).
# Pinning here floors both. Revisit when fastapi tightens its own range.
starlette==1.3.1
uvicorn[standard]==0.51.0
jinja2==3.1.6
httpx==0.28.1
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
itsdangerous==2.2.0
python-dotenv==1.2.2
aiosmtplib==5.1.2
markdown==3.10.2
authlib==1.7.2
