x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  setlist-stash:
    build: .
    image: setlist-stash:0.3.0
    container_name: setlist-stash
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=16M
    ports:
      - "${APP_PORT:-3706}:${APP_PORT:-3706}"
    env_file:
      - .env
    environment:
      APP_HOST: "0.0.0.0"
      APP_PORT: "${APP_PORT:-3706}"
      TZ: "${TZ:-UTC}"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - default
      # Phase 4: join the mcp-phish container's default network so the game
      # can reach `mcp-phish` by name. The game NEVER joins phish-vault_default.
      # Vault reads route through mcp-phish only.
      - mcp-phish
    logging: *default-logging
    healthcheck:
      test: ["CMD", "python", "-m", "setlist_stash.healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    security_opt:
      - no-new-privileges:true

  setlist-stash-resolver:
    # Same image as setlist-stash; different command. Inside-container loop
    # (docs/PHASE-4-PLAN.md §5 Option B). Sleeps RESOLVER_INTERVAL_SECONDS between
    # ticks. Restart policy + healthcheck restart it on transient failures
    # without scheduling burden on the host.
    build: .
    image: setlist-stash:0.3.0
    container_name: setlist-stash-resolver
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=16M
    env_file:
      - .env
    environment:
      TZ: "${TZ:-UTC}"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - default
      - mcp-phish
    entrypoint: ["python", "-m", "setlist_stash.resolve", "--loop"]
    # The Dockerfile inherits a /healthz HEALTHCHECK from the FastAPI app.
    # The resolver container does not run the HTTP server, so disable it.
    # Container liveness here is "process is running"; tick health is
    # surfaced via /healthz on the main app via scoring_runs.
    healthcheck:
      disable: true
    logging: *default-logging
    security_opt:
      - no-new-privileges:true

  setlist-stash-reminders:
    # Same image as the app; different command. Sleeps until
    # REMINDER_HOUR_LOCAL in DISPLAY_TZ, then mails show-day pick reminders to
    # verified, non-opted-out players who have not picked yet.
    #
    # Its own service rather than a branch inside the resolver loop: the
    # resolver's cadence swings between 60s and 30 minutes around a show, so
    # "fire at 09:00" would land up to half an hour late there, and a fault in
    # reminder mail must not be able to stall the loop that scores the game.
    #
    # Inert unless REMINDER_ENABLED=true in this deployment's .env, so the OSS
    # image and any third-party self-host run it as a sleeping no-op.
    build: .
    image: setlist-stash:0.3.0
    container_name: setlist-stash-reminders
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:size=16M
    env_file:
      - .env
    environment:
      TZ: "${TZ:-UTC}"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - default
      - mcp-phish
    entrypoint: ["python", "-m", "setlist_stash.reminders", "--loop"]
    # Same reasoning as the resolver: the image's inherited HTTP healthcheck
    # targets the FastAPI app, which this container does not run.
    healthcheck:
      disable: true
    logging: *default-logging
    security_opt:
      - no-new-privileges:true

  postgres:
    image: postgres:16-alpine
    container_name: setlist-stash-pg
    restart: unless-stopped
    environment:
      POSTGRES_DB: "${PG_DB:-setlist_stash}"
      POSTGRES_USER: "${PG_USER:-setlist_stash}"
      POSTGRES_PASSWORD: "${PG_PASSWORD:?PG_PASSWORD must be set in .env}"
    volumes:
      - setlist-stash-pg-data:/var/lib/postgresql/data
    # Loopback-only host binding for ad-hoc queries; cross-container traffic
    # uses the docker bridge.
    ports:
      - "127.0.0.1:5434:5432"
    healthcheck:
      test:
        ["CMD-SHELL", "pg_isready -U ${PG_USER:-setlist_stash} -d ${PG_DB:-setlist_stash}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    logging: *default-logging

volumes:
  setlist-stash-pg-data:

networks:
  mcp-phish:
    external: true
    name: mcp-phish_default
