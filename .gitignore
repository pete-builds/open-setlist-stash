# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.venv/
venv/

# Local env / secrets
.env
.env.local
*.local.yml

# Compose override (deployment-specific; mount private themes/templates/etc here)
docker-compose.override.yml
docker-compose.override.yaml

# Private themes / preview drafts (drop your custom CSS here without committing)
src/setlist_stash/static/themes/*.css
preview.local.html

# Operator-only files (never committed)
SESSION-RESUME.md
PROBE-NOTES.md

# Editor / OS
.idea/
.vscode/
.DS_Store

# Build artifacts
dist/
build/
